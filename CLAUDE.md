## Agent skills

### Issue tracker

Local markdown — issues/PRDs live under `plans/issues/<feature-slug>/`. See `plans/agents/issue-tracker.md`.

### Triage labels

Default vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `plans/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `plans/ADR/` at the repo root. See `plans/agents/domain.md`.
